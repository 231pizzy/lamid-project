'use client'

import { Box } from "@mui/material"

export default function FileUpload({ handleFiles, multiple, accept, extentionArray, showFiles, viewUploadedContact, removeContact, fileArray, saveName, filenameIsEditable }) {
    return <Box>
        file upload
    </Box>
}